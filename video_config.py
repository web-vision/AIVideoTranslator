CONFIG = {
    "default_speaker": "p230",              # e.g. male voice
    "subtitle_mode": "soft",                # soft = overlay as text, hard = baked into the video
    "temp_dir": "temp",
    "bulk_pause_after_glossary": False,     # True = Bulk mode - Pause after the glossary step, might be useful, if you want to chek the SRT files and make some manual corrections.
    "manual_pause_after_glossary": False    # True = Single mode - Make a pause after the glossary step, might be useful, if you want to chek the SRT files and make some manual corrections.
}